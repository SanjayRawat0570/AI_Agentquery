fastapi==0.104.1
uvicorn==0.24.0
python-dotenv==1.0.0
requests==2.31.0
markdown==3.5.1
pydantic==1.10.24
pytest==7.4.0

# Phase 1 Dependencies (compatible versions)
sqlalchemy>=2.0.29
alembic>=1.13.1

# Optional: PostgreSQL support (requires Visual C++ build tools on Windows)
# psycopg2-binary==2.9.9
# Note: SQLite works out of the box without additional dependencies
